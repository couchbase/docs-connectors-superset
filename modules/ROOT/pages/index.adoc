= Introduction
:page-topic-type: guide
:page-toclevels: 2
:description: The Couchbase SQLAlchemy Connector provides data visualization and exploration for the Couchbase platform, using Apache Superset.

[abstract]
--
{description}

The Couchbase SQLAlchemy Connector integrates Couchbase Analytics tabular views and Capella Columnar instances with the open source data exploration and visualization platform Apache Superset.
--

== Install the Couchbase SQLAlchemy Connector

To install the Couchbase SQLAlchemy Connector, open the command prompt (`cmd`), and enter the following:

[source, console]
----
pip install couchbase-sqlalchemy
----

== Install Apache Superset

Download and install https://superset.apache.org/docs/quickstart[Apache Superset] following the official documentation.

== Setting a Secure Connection

Couchbase *strongly* recommends that you secure your Superset connection using SSL.
This ensures encryption of the communication between SQLAlchemy and the Couchbase Server/Capella.

[tabs]
====
Capella Operational::
+
--
You need to get a connection string for your database
and generate an API key to plug into the Superset connector.

[#copy-connection-string]#To obtain the `connection string`.#

. Sign in to your Capella instance as an Administrator.
. Select the menu:Connect[] item from the top-level menu.
. Make a copy of the `public connection string`.
+
image::get-connection-string.png[copying connection string]
+
. In the next section of the page, you'll find the link to create a list of
menu:Allowed IP Addresses[].
Click on the link to create an entry for the IP address of the machine from which you're running Power BI.
+
image::allowed-ip-addresses.png[creating-allowed-ip address]
+
TIP: You can use the btn:[Add Current IP Address] button to fill in the address of the machine currently running the web console.

. [#setup-database-access]#Return to the *Connect* screen and click on btn:[Database Access] link.#
Create a new database access entry, setting values for `bucket`, and `scopes`.
+
image::create-database-access.png[Creating database access]

. To set up a secure connection, you'll need to retrieve the root certificate for your Capella instance.
+
.. Click on btn:[Security Certificate] in the left menu.
.. Click on btn:[Download] to transfer a copy of the root certificate to your local machine.
+
image::download-root-certificate.png[]
--

Capella Columnar::
+
--

For details on creating a Capella Columnar instance,
first see xref:columnar:admin:prepare-project.adoc[Creating a Capella Columnar Cluster].

[#capella-columnar-connection-string]#You'll need to access the Capella administration console to get the connection string for your Columnar database.#

. Sign in to your Capella instance as an `Organization owner` or `Project owner`.
. Select menu:Columnar[] from the top-level page menu.
. Select your Columnar cluster from the list.
. Select menu:Settings[] from the top-level page menu.
+
image::select-columnar-cluster-settings.png[]
+
. Select menu:Connection String[] from the left-hand menu.
+
image::get-capella-columnar-connection-string.png[]
+
. Make a note of the connection string.

Next, you need to add the IP address of the machine from which you're running Power BI,
 so that Capella allows the machine to access the Columnar data.

. Click on menu:Allowed IP Addresses[] in the left-hand menu.
. Click the btn:[Add Allowed IP] button.

+
image::capella-columnar-add-allowed-ip.png[]
+
. Enter the IP address of the Power BI host machine.
+
TIP: You can use the btn:[Add Current IP Address] button to fill in the address of the machine currently running the web console.

[#capella-columnar-user-account]#Now, you will need to create a user account for Power BI to access the columnar data.#

. Return to the menu:Settings[] page, then click on menu:Access Control[] in the left-hand menu.
. Click on btn:[Create Account].
. Add a `username` and `password` for the new account.
. Make sure you set assign  `sys_view_reader` to the list of roles.
+
image::capella-columnar-user-account.png[]
+
. Click btn:[Create Account] to finish setting up the user account.
--

Self-Managed Couchbase Server::
+
--

[#download-root-certificate]#To create a secure connection to a self-managed Couchbase Server instance, you need to get the root certificate from the Administrator's Console.#

. Use your web browser to access the Admin console and log in as an Administrator.
. Use the left-hand menu to access the menu:Security[] settings.
. From the top menu, select menu:Certificates[]. This takes you to the page from which you can view the server's security certificates:
+
image::access-root-certificate.png[]
+
. Copy *all* the text in the Trusted *Root Certificates* section (start from `BEGIN CERTIFICATE` and include `END CERTIFICATE`),
and save the text to an accessible `PEM` file.
E.g., `C:\Users\user\certificate.pem`.
+
You will use this file later when you create the Superset connection.
--

====

== Configure the Connection in Superset

Launch Apache Superset and set up a new database connection. 
Select Couchbase as the database type and provide the necessary connection details.

[tabs]
====

Capella Operational::
+
--
. Open the Superset portal.
. Add Database Connection:
  . Go to: Settings → Database Connections → Add Database
  . Select Couchbase from the list of databases.

Fill connection details:

[horizontal]
*Host*:: Fill in the connection string you copied here.

*Port*:: Not required.

*Database Name*:: Optional; specify the database name if needed.

*Username*:: Enter the username you created in the User Account section.

*Password*:: Enter the password you allocated to the user here.

*Display Name*:: Enter a name for this connection.

*Additional Parameters*:: Not required.

*SSL*:: Must be enabled.

--

Capella Columnar::
+
--
. Open the Superset portal.
. Add Database Connection:
  . Go to: Settings → Database Connections → Add Database
  . Select Couchbase from the list of databases.

Fill connection details:

[horizontal]
*Host*:: Fill in the connection string you copied here.

*Port*:: Not required.

*Database Name*:: Optional; specify the database name if needed.

*Username*:: Enter the username you created in the User Account section.

*Password*:: Enter the password you allocated to the user here.

*Display Name*:: Enter a name for this connection.

*Additional Parameters*:: Not required.

*SSL*:: Must be enabled.

--

Self-Managed Couchbase Server::
+
--
. Open the Superset portal.
. Add Database Connection: 
  . Go to: Settings → Database Connections → Add Database
  . Select Couchbase from the list of databases.

Fill Connection Details:

[horizontal]
*Host*:: Enter Couchbase cluster IP address.

*Port*:: Use 11207 for a secure connection.

*Database Name*:: Optional; specify target database if needed.

*Username*:: Enter user with CBAS permissions (e.g., Cluster Admin).

*Password*:: Enter the cluster password.

*Display Name*:: Enter a name for this connection.

*Additional Parameters*:: For certificates, enter: truststorepath=C:\path\to\certificate.txt

*SSL*:: Enable if using SSL, otherwise ignore.
--

====

== Create Tabular Analytics View

In Couchbase Analytics, define https://docs.couchbase.com/columnar/sqlpp/5a_views.html#TAV[Tabular Analytics Views]. These views act as non-materialized views, specifying schemas along with primary and foreign keys for collections or subqueries.


== Use the Couchbase Superset Connector

. To create tabular views for graphical visualization, start by loading data into a collection. 

For this example, you'll use a dataset called https://www.kaggle.com/datasets/gregorut/videogamesales[video_game_sales.json].

. Make sure your `video_game_sales.json` file is accessible in a location that your database system can read from. 
This file contains structured JSON data about video game sales.

. Create External dataset using `video_game_sales`.

. On querying Select * from `Default.Default.video_game_sales limit 1`. 

+
[source, console]
----
[
  {
   "video_game_sales": {
     "_columnar_ID": "691c68ce-1dd3-9e94-7a47-c041eb87fe8c",
     "rank": "74",
     "name": "Animal Crossing: New Leaf",
     "platform": "3DS",
     "year": "2012",
     "genre": "Simulation",
     "publisher": "Nintendo",
     "na_sales": "2.01",
     "eu_sales": "2.32",
     "jp_sales": "4.36",
     "other_sales": "0.41",
     "global_sales": "9.09"
   }
 }
]
----

. To create a tabular analytics view from the `video_game_sales` collection, execute the following command:
+
[source, console]
----
CREATE OR REPLACE ANALYTICS VIEW
video_game_sales_1 (_columnar_ID string, rank int, name string, platform string, year string, genre string, publisher string, na_sales double, eu_sales double, jp_sales double, other_sales double, global_sales double)
DEFAULT NULL PRIMARY KEY (__id)
NOT ENFORCED AS
SELECT video_game_sales.* FROM video_game_sales;
----

=== Visualization

To visualize the data, follow these steps in Apache Superset:

. Navigate to the Dataset section in the Superset dashboard.
. Select `Couchbase-Analytics-With-SSL` as the database.
. Choose *Default* as the schema.
. Look for the view named *video_game_sales_1* to proceed with the data visualization.

*Sample Chart 1 - Bar Chart Legacy*

image::chart-1-bar-chart-legacy.png["Bar Chart Legacy"]


*Sample Chart 2 -  Tree Map*

image::chart-2-tree-map.png["Tree map"]

